C:\参考ソースコードUTF-8版>java CalcWorkingTime 9:00:00 18:00:00
本日の労働時間は9時間0分です。

final long ONE_HOUR_BY_MILLI_SEC = 1000 * 60 * 60;
出勤時刻を表す文字列（例：『08:00:00』）と
退勤時刻を表す文字列（例：『18:00:00』）を
コマンドライン引数として受け取り、
それぞれTimeオブジェクト（時刻情報を扱うデータ。現時点ではTime型という特殊な型の変数と思っていただければOK）に変換します。
変換したTimeオブジェクトには標準時刻（日本の場合は午前9時）
からの経過時間がミリ秒（1000分の1秒）単位で格納されます。
『08:00:00』を入力 → 『-3600000』が格納される
『18:00:00』を入力 → 『32400000』が格納される

なお、Timeオブジェクトを利用するには「java.sql.Time」のインポートが必要です

Time startTime  = Time.valueOf(args[0]);
Time finishTime = Time.valueOf(args[1]);

TimeオブジェクトはgetTimeメソッドを使うことで格納されている時間データを
long型で取得できます。
finishTime（退勤時刻データ）からstartTime（出勤時刻データ）を差し引く
ことでその日の労働時間をミリ秒単位で取得することができます

int workingHour = (int)( workingTime / ONE_HOUR_BY_MILLI_SEC );                      
int workingMin  = (int)( ( workingTime / ONE_MIN_BY_MILLI_SEC ) % ONE_HOUR_BY_MIN );





取得した労働時間（ミリ秒単位）を「◯時間△分」の形に変換します。
「◯時間」の部分は取得した労働時間をミリ秒から時間単位に直して端数を
排除することで、「△分」の部分は取得した労働時間を
分単位に直して60で割った余りを求めることで、
それぞれ取得することが可能です。



今回読み込むCSVファイルと呼ばれる外部ファイルです。
（ファイルの拡張子が『.csv 』のファイル）
CSVは『Comma Separated Value』の略で、複数の値を『, 』（カンマ）で
区切った表形式のデータを扱います



プログラムからCSVファイルにアクセスし、1行分ずつ文字列を
読み込んで順次ArrayListに格納していきます。
読み込みにはAPIクラス『File』『FileReader』『BufferedReader』が
必要になります。
これらの使用には以下のインポートが必要になります。- java.io.BufferedReader- java.io.File- java.io.FileReader
また、これらを使用するコードは
『try{ 』と『}catch(IOException e){ System.out.println(e); } 』
で囲う必要があります。




java.sql.Timeクラスのオブジェクトは、時刻情報のみを扱うため、日付情報を含むことはありません。



理由は、Time.valueOf(String s)メソッドが引数として渡された文字列形式の時刻データをjava.sql.Timeオブジェクトに変換する


「60 * 60 * 1000」は、1時間をミリ秒で表すための正しい計算式です。
3600000ミリ秒が1時間に相当するため、この値で労働時間を割ることで、ミリ秒単位から時間単位への変換が正しく行えます。



「getTime」はTimeオブジェクトからミリ秒単位で時刻情報を取得するため、労働時間の計算に必須です。


forSplitRecode配列の最初の要素が日付データ「2020-01-01」を含んでいるためで、
Date.valueOfメソッドでこの要素を渡すことで、文字列形式の日付をDateオブジェクトに変換できます。



あなたが選んだ「split()」メソッドは、カンマで区切られた文字列を分割して配列として返すため、
ファイルから読み込んだ行を処理するのに最適です。このメソッドを使うことで、CSVデータなどを簡単に扱うことができます。

「ArrayList」は、動的サイズの配列を実装しており、ファイルから読み込んだ各行を簡単に追加できるため、
データの管理が非常に便利になります。


FileReaderクラスは、ファイルから文字データを読み込むために特化して設計されているため、ファイル処理において非常に便利です。


BufferedReaderは、テキストデータを効率よく処理するために文字単位でデータを読み込むクラスであり、
特にテキストファイルの読み込みに適しています。


splitメソッドはJavaの正規表現に対応しており、複雑な文字列パターンを使って文字列を柔軟に分割できるため、非常に便利です。














